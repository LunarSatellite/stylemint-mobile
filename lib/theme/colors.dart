import 'package:flutter/material.dart';

// ── Brand ────────────────────────────────────────────────────────────────────
const kPrimaryColor      = Color(0xFF7C3AED); // violet-600 — Style Mint primary
const kPrimaryLight      = Color(0xFFEDE9FE); // violet-100
const kPrimaryDark       = Color(0xFF5B21B6); // violet-800
const kSecondaryColor    = Color(0xFFF59E0B); // amber-400
const kAccentColor       = Color(0xFFEC4899); // pink-500

// ── Neutrals ─────────────────────────────────────────────────────────────────
const kScaffoldBg        = Color(0xFFFFFFFF);
const kScaffoldBgDark    = Color(0xFF0F0F0F);
const kSurfaceColor      = Color(0xFFFFFFFF);
const kSurfaceColorDark  = Color(0xFF1C1C1E);

// ── Text ─────────────────────────────────────────────────────────────────────
const kTextColor         = Color(0xFF111827); // gray-900
const kTextSecondary     = Color(0xFF6B7280); // gray-500
const kHintTextColor     = Color(0xFF9CA3AF); // gray-400
const kTextInverse       = Color(0xFFFFFFFF);

// ── Borders / dividers ───────────────────────────────────────────────────────
const kBorderColor       = Color(0xFFE5E7EB); // gray-200
const kDividerColor      = Color(0xFFF3F4F6); // gray-100

// ── Status ───────────────────────────────────────────────────────────────────
const kSuccessColor      = Color(0xFF22C55E); // green-500
const kWarningColor      = Color(0xFFF97316); // orange-500
const kErrorColor        = Color(0xFFEF4444); // red-500
const kInfoColor         = Color(0xFF3B82F6); // blue-500

// ── Grey scale ───────────────────────────────────────────────────────────────
const kGrey50            = Color(0xFFF9FAFB);
const kGrey100           = Color(0xFFF3F4F6);
const kGrey200           = Color(0xFFE5E7EB);
const kGrey300           = Color(0xFFD1D5DB);
const kGrey400           = Color(0xFF9CA3AF);
const kGrey500           = Color(0xFF6B7280);
const kGrey600           = Color(0xFF4B5563);
const kGrey700           = Color(0xFF374151);
const kGrey800           = Color(0xFF1F2937);
const kGrey900           = Color(0xFF111827);

// ── Shimmer ──────────────────────────────────────────────────────────────────
const kShimmerBase       = Color(0xFFEBEBF4);
const kShimmerHighlight  = Color(0xFFF4F4F4);
